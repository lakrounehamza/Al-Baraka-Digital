spring:
  application:
    name: Al-Baraka-Digital

  datasource:
    url: jdbc:mysql://localhost:3307/albaraka
    username: username
    password: userpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
